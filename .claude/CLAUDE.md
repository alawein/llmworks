---
type: local-claude-config
generated: true
source: claude-agent-platform-extender
---

<!-- .claude/CLAUDE.md generated by Extender on 20260428-144718 -->
<!-- Rationale: Local instructions encode repository-specific commands and conventions. -->

# Project Claude Configuration: llmworks

## Detected Project Profile

- Root: `/c/Users/mesha/Desktop/Dropbox/GitHub/alawein/llmworks`
- Project type: `node`
- Stack: `node,typescript`
- Frameworks: `react`
- Manifests: `package.json`
- CI: `github-actions`
- Tools: `vite,vitest,playwright,docker`
- Source dirs: `src`
- Test dirs: `tests,e2e`

## Project Commands

- Test command: `npm run test`
- Build command: `npm run build`
- Lint command: `npm run lint`
- Package manager: `npm`

## Local Precedence

- Prefer this file over user-level defaults for project commands. <!-- Rationale: Detected repo commands are more specific than global defaults. -->
- Preserve global safety gates from `~/.claude/CLAUDE.md`. <!-- Rationale: Local overrides should not disable safety. -->
- Avoid same-name local skills unless intentionally shadowing global behavior. <!-- Rationale: Skill conflict resolution can be surprising. -->

## Local Agent Specializations

### Orchestrator

- For multi-step work, run lint/test/build commands above when relevant.
- If command is `unknown`, ask once for the missing command and store it in `.claude/MEMORY.md`.

### Codex

- Match patterns from: `src`.
- Use the detected stack: `node,typescript` and frameworks: `react`.
- Generate tests near existing test dirs when present: `tests,e2e`.

### Cursor

- Use surgical diffs.
- After edits, run: `npm run test` when not `unknown`.
- Do not reformat unrelated files.

### Research

- Prefer local patterns in `src` before external docs.
- Use manifests `package.json` to infer versions before recommending APIs.

### Reviewer

- Treat changes to manifests, CI, auth, migrations, and lockfiles as high-risk.
- BLOCKER findings prevent `/commit` and `/pr` workflows.

### Extender

- Re-scan with: `~/.claude/bin/generate-local-claude.sh --root . --dry-run --force`.
- Approve regenerated config only after reviewing the diff.

## Local Workflows

### PR Ready

1. Run lint: `npm run lint` if known.
2. Run tests: `npm run test` if known.
3. Run build: `npm run build` if known.
4. Run Reviewer on the diff.
5. Run security-scan on high-risk files.
6. Draft changelog, commit, and PR only after approval.

### New Feature

1. Research local patterns.
2. Scaffold minimal structure.
3. Implement behavior.
4. Generate tests.
5. Update docs.
6. Review and run PR Ready.

### Bug Fix

1. Reproduce failure.
2. Identify root cause.
3. Patch minimally.
4. Add regression test.
5. Run relevant tests.
6. Review and commit.

## Local Skills

- Store repo-scoped skills in `.claude/skills/<skill-name>/SKILL.md`.
- Version local skills in `.claude/skills/registry.json`.
- Conflict detection result: `none`.

## Local Memory

- Store architecture decisions as `[DECISION]`.
- Store discovered code patterns as `[PATTERN]`.
- Store command corrections as `[CONFIG]`.
- Store pitfalls as `[GOTCHA]`.
- Keep session summaries to five lines max.

## Org Policy Overlay

<!-- Added by Claude Agent Platform bootstrap 2026-04-28 -->
- org: alawein
- risk_tier: medium
- default_action: propose
- require_approval_for: commit, push, force-push, ci_changes, secret_scans
- block_on_blocker_findings: false
- review_severity_floor: medium
- secrets_scan_strictness: standard
- allowed_branches: main, master, dev
- forbidden_paths: **/.env*, **/secrets.*, **/credentials*, **/keys/*
- default_workflow: pr-ready

